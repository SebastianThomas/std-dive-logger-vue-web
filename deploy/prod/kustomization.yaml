apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: std-dive-logger-prod
resources:
  - ../base
# Hostnames are the base ones, unpatched: std-dive-logger-web.sthomas.ch and
# std-dive-logger.sthomas.ch (the bare project host now serves the app).
