---
# Placeholder name - the dev/ and prod/ overlays rewrite it via `namespace:`.
# Shared with the backend repo's base - keep the two identical.
apiVersion: v1
kind: Namespace
metadata:
  name: std-dive-logger
