// Shared sort types for server-side sorting in Dive List and related views
export type SortDirection = 'ASCENDING' | 'DESCENDING'

// Server-supported sortable columns
export type SortColumn = 'ID' | 'NUMBER' | 'CUSTOM_IDENTIFIER'
